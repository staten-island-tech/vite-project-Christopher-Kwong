/* import { DOM } from "./dom";
import { Array } from "./array";

function FilterNorthAmerica() {
  Array.filter((element) => element.Continent.includes("NA")).forEach(
    (element) => console.log(element.Name)
  );
}
 */
